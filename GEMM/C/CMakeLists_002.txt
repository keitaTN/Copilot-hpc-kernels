This file contains the outputs generated from trying 
to create a CMakeLists.txt file for GEMM_002


/** Attempt 1 */
// generate CMakeLists.txt for ./GEMM_002.c

=======

// generate CMakeLists.txt for ./GEMM_002.cpp

// using 3.20.1

// by calling: ./cmakegen -cmake -file ./GEMM_002.cpp -target GEMM_002

// generated at: 2021-10-16 17:58:05

// for more information, see:


/** Attempt 2 */
## write CMake file to build GEMM_002.c

cmake_minimum_required(VERSION 3.5)

project(gemm_002)

set(CMAKE_CXX_STANDARD 11)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

add_executable(gemm_002 gemm_002.cpp)

target_link_libraries(gemm_002 mkl_intel_lp64 mkl_core mkl_gnu_thread pthread m dl)

## Write CMake file to build GEMM_002.c with OpenMP
cmake_minimum_required(VERSION 3.5)

project(gemm_002_omp)

set(CMAKE_CXX_STANDARD 11)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

add_executable(gemm_002_omp gemm_002.cpp)

target_link_libraries(gemm_002_omp mkl_intel_lp64 mkl_core mkl_gnu_thread pthread m dl)

target_compile_options(gemm_002_omp PUBLIC -fopenmp)
target_link_libraries(gemm_002_omp PUBLIC -fopenmp)


/** Attempt 3 */
## Write CMake file to build GEMM_002.c with MKL
cmake_minimum_required(VERSION 3.5)

project(gemm_002_mkl)

set(CMAKE_CXX_STANDARD 11)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

add_executable(gemm_002_mkl gemm_002.cpp)

target_link_libraries(gemm_002_mkl mkl_intel_lp64 mkl_core mkl_gnu_thread pthread m dl)

target_compile_options(gemm_002_mkl PUBLIC -DMKL)
target_link_libraries(gemm_002_mkl PUBLIC -DMKL)



## write CMake file build for GEMM_002.c
cmake_minimum_required(VERSION 2.8.12 FATAL_ERROR)

project(GEMM_002 LANGUAGES CXX)

set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -O3 -std=c++11")

set(CMAKE_CXX_FLAGS_DEBUG "${CMAKE_CXX_FLAGS_DEBUG} -O0 -g")

set(CMAKE_CXX_FLAGS_RELEASE "${CMAKE_CXX_FLAGS_RELEASE} -O3")

add_executable(GEMM_002 GEMM_002.c)

## build

mkdir build

cd build

cmake -DCMAKE_BUILD_TYPE=Release ..

make

## run

./GEMM_002

## output

M=1000, N=1000, K=1000

time